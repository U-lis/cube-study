# Phase 3 — 테스트 계획

## 단위 테스트

### T3-1. 판정 함수 (`grade`)

| # | 목표 케이스 | 입력 알고리즘 | 기대 Verdict |
|---|---|---|---|
| 1 | `LB` | `R D2 R' U R D2 R' U'` (데이터의 direct) | `correct` |
| 2 | `LB` | 데이터의 setup.alg | `correct` |
| 3 | `LB` | 데이터의 strict.alg (상쇄 전) | `correct` |
| 4 | `LB` | `R R'` | `identity` |
| 5 | `LB` | `` (빈 문자열) | `identity` |
| 6 | `LB` | `R U R' U'` | `wrong` (엣지도 건드리는 비-3cycle) |
| 7 | `LB` | 다른 케이스(`TU`)의 알고리즘 | `wrong`, identified=`TU` |
| 8 | `LB` | `BL`(역케이스) 알고리즘 | `wrong`, identified=`BL` |
| 9 | `LB` | 코너는 LB를 풀지만 엣지를 건드리는 알고리즘 | `edge-dirty`, cubies 비어있지 않음 |
| 10 | 전수 378 | 각 케이스의 direct.alg | 378/378 `correct` |
| 11 | 전수 378 | 각 케이스의 setup.alg | 378/378 `correct` |
| 12 | 전수 378 | 각 케이스의 direct.strict.alg | 378/378 `correct` |

**#9 알고리즘 확보 방법**: 정답 알고리즘 앞뒤에 `M`을 붙이지 말고(3-cycle이 깨짐), `LB` 정답에 엣지만 바꾸는 짝수 순열을 덧붙여 만든다. 테스트 작성 시 시뮬레이터로 조건 충족을 먼저 확인한 뒤 상수로 박는다.

### T3-2. 변형 알고리즘 정답 처리 (FR-18 핵심)

| # | 검사 | 기대 |
|---|---|---|
| 1 | 데이터에 없는 유효 커뮤테이터로 `LB` 풀기 | `correct` |
| 2 | 정답 알고리즘 앞뒤에 `U U'` 삽입 | `correct` (효과 동일) |
| 3 | 정답 알고리즘을 `U`로 conjugate한 것 | `wrong` (다른 케이스가 됨) |

### T3-3. 무브 입력 상태

| # | 조작 | 기대 |
|---|---|---|
| 1 | `R` `U` 누름 | `moves = ['R','U']`, alg `"R U"` |
| 2 | 되돌리기 | `moves = ['R']` |
| 3 | 전체 지우기 | `moves = []` |
| 4 | 200회 입력 후 추가 입력 | 무시됨 |
| 5 | 빈 상태에서 되돌리기 | 오류 없이 무시 |

## E2E 테스트 (Playwright)

### T3-4. 퀴즈 흐름

| # | 시나리오 | 기대 |
|---|---|---|
| 1 | `/quiz` 진입 | 케이스 코드와 타깃 2개 표시 |
| 2 | 판정 전 DOM | 정답 알고리즘 문자열이 존재하지 않음 |
| 3 | 무브 버튼 18개 | 모두 표시, 각 44px 이상 |
| 4 | 정답 입력 후 제출 | `정답` 표시 + direct/setup 알고리즘 공개 |
| 5 | 오답 입력 후 제출 | 해당 Verdict 문구 표시 |
| 6 | 빈 입력 | 제출 버튼 비활성 |
| 7 | 조회 화면 링크 클릭 | `/?c={code}` 로 이동하며 해당 케이스 표시 |
| 8 | "다음 문제" | 새 케이스로 교체 (직전과 다름) |

### T3-5. NFR-9 확인

| # | 검사 | 기대 |
|---|---|---|
| 1 | 정답 시 표시 문구 | `정답`만. 축하·격려 문구 없음 |
| 2 | 오답 시 표시 문구 | 사실 진술만. 재도전 권유 없음 |
| 3 | 연속 정답 카운터·배지 | 존재하지 않음 |

## 완료 조건

- [ ] `grade`가 378케이스 × 3표현(direct/setup/strict) 전부 `correct` (1134건)
- [ ] 5가지 Verdict가 각각 실제로 발생하는 입력으로 검증됨
- [ ] 판정 전 정답이 DOM에 없음
- [ ] E2E 통과 (Playwright 실행 가능한 경우)
