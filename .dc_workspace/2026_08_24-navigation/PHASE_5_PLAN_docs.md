# Phase 5 — 문서화 (docs)

**담당**: FR-NAV-15, 16; SPEC "결정된 것 6, 7"; SPEC 부록의 두 목차
**의존**: Phase 4 완료 (규약이 검사로 성립하는지 드러난 뒤)
**병렬 불가**: 순차

## 목표

이 페이즈가 끝나면:
- `.dc_workspace/CONVENTIONS.md` 신설. 화면 규약의 정본
- 저장소 루트 `CLAUDE.md` 신설. 매 세션 실리는 규칙 요약 + 문서 지도
- `README.md:111` 에 `CONVENTIONS.md` 링크 추가
- `CHANGELOG.md` 에 `## [0.5.0]` 항목 (없으면 `deploy/release.sh` 가 릴리스를 거부)
- `SVELTE.md` 는 **손대지 않는다** (이미 정본)

**페이즈 5 는 다른 페이즈와 같은 페이즈다** (SPEC "페이즈 제안" 각주). `/dotclaude:update-docs` 를 쓰지 않는다 — 규약의 정본 두 개를 만드는 것이라 설계와 검토를 거쳐야 한다.

## 선행 조건

- Phase 4 완료. 어떤 규약이 자동으로 서 있고 어떤 것이 사람 몫인지 실측으로 드러남

## 대응 SPEC

| FR | 내용 |
|---|---|
| FR-NAV-15 | `.dc_workspace/CONVENTIONS.md` 신설. 흩어져 있는 규약을 옮김 |
| FR-NAV-16 | 저장소 루트 `CLAUDE.md` 신설. 규칙 담고 상세는 링크 |

## 커밋 단위 (권장)

1. `docs(convention): 화면 규약을 .dc_workspace/CONVENTIONS.md 로 모은다`
2. `docs(claude): 저장소 루트 CLAUDE.md 로 세션 규칙을 편다`
3. `docs(changelog): 0.5.0 항목을 낸다`

세 커밋을 나누는 이유는 되돌리기 단위가 다르기 때문이다 — CONVENTIONS 는 방금 검증한 규약, CLAUDE.md 는 그것으로 가는 지도, CHANGELOG 는 릴리스 절차 준비.

## 수정·생성할 파일

### 1. `.dc_workspace/CONVENTIONS.md` (신규)

SPEC 부록의 목차를 그대로 쓴다 (SPEC "부록 — 페이즈 5 에서 만들 문서의 목차 · CONVENTIONS.md").

- [ ] 형식: `dotclaude-config` 헤더 **없이**. `BACKLOG.md` · `CONTINUE.md` 처럼 그냥 마크다운 (SPEC 근거)
- [ ] 각 규약은 **규칙 · 근거 · 검사** 세 줄
- [ ] 검사가 없는 것은 "검사 없음 — 리뷰에서 본다" 로 표시 (FR-NAV-17)

목차 (SPEC 부록):

```
# 화면 규약

## 1. 구조
   1.1 화면은 자리가 고정된 영역의 세로 나열
   1.2 <h1> 은 화면 이름이고 화면당 하나
   1.3 설정은 화면 이름과 본문 사이, 진행 버튼은 본문 최하단
   1.4 하단 탭은 같은 카테고리의 형제만. 형제가 없으면 그리지 않는다

## 2. 흐름
   2.1 단계 이름 idle → active → input → result, data-stage 로 노출
   2.2 단계 이름은 공유하되 단계를 강제하지 않는다
   2.3 측정하는 판이 도는 동안만 설정을 접고 잠근다
   2.4 접는 것(CSS)과 잠그는 것(disabled)은 따로 한다
   2.5 단계로 안 갈리는 조건은 따로 속성으로 낸다 (data-input-open)
   2.6 단계가 바뀌어 화면이 짧아지면 스크롤을 원점으로

## 2.5 세로
   2.7 접는 것이 기본, 자리 예약은 하이드레이션이 흔들리는 자리에만
   2.8 세로 예산은 가변 영역 하나가 clamp(하한, 100svh - 고정분, 상한) 로 흡수
   2.9 고정분은 상단 바를 포함하고 단계별 고정 높이 중 큰 쪽
   2.10 정사각 영역은 폭에도 묶인다

## 3. 표기
   3.1 판정 줄 — data-verdict / data-kind / data-result
   3.2 진행 버튼 — data-{동작}, 자리는 본문 최하단. 패드 안 편집 버튼은 data-action
   3.3 색만으로 정보를 전달하지 않는다
   3.4 터치 타깃 최소 44px
   3.5 UI 문구는 사실만
   3.6 한국어 UI, 큐브 표기와 케이스 코드는 영문

## 4. 렌더
   4.1 프리렌더/하이드레이션 요소 개수·크기 불변
   4.2 알고리즘 렌더는 Alg.svelte 한 곳

## 5. 저장
   5.1 localStorage 키와 스키마 버전을 기능별로 분리
   5.2 데이터 불변 — 알고리즘을 코드가 만들지 않는다

## 검사 목록
   각 규칙 → 그것을 지키는 테스트 파일:라인
```

각 규칙 항목에 넣을 것:

- **규칙** — 한 문장의 명령형
- **근거** — 왜 (어느 함정을 밟았는가, 어느 SPEC 이 정본인가)
- **검사** — 파일:라인 (`tests/e2e/conventions.spec.ts:XX` 나 "검사 없음 — 리뷰")

**GLOBAL 이 못 박은 예외를 함께 적음**:

- 규칙 1.3 (설정 자리): **예외** — 기준 상세의 목록 필터 두 개, 트레이싱의 세션 설정. 왜: 판의 설정이 아니라 표시 필터이거나 (기준 상세), 큐브가 h1 바로 아래에 서야 하는 흐름 (트레이싱)
- 규칙 3.2 (진행 버튼 CSS 접기): **예외** — 퀴즈의 진행 버튼은 `{#if verdict}` 로 다른 버튼을 렌더. 근거: `verdict` 는 브라우저 상태에서 오지 않으므로 SSR/CSR 이 갈릴 위험이 없다. 단계별 하나만 보이는 상위 규약은 지켜진다

**옮겨 오는 것이지 새로 쓰는 것이 아니다** (FR-NAV-15). 각 규칙의 근거는 이미 있는 문서·주석에서 인용한다. SPEC "출처" 표:

| 규약 | 지금 있는 곳 |
|---|---|
| 프리렌더/하이드레이션 불변 (4.1) | `2026_08_08/GLOBAL.md` AD-4, `2026_08_20/GLOBAL.md` AD-14 |
| UI 문구 사실만 (3.5) | NFR-9, `2026_08_03/GLOBAL.md` 6절 |
| 이중 부호화 (3.3) | #26 |
| 터치 44px (3.4) | FR-MC-4 |
| localStorage 분리 (5.1) | `2026_08_20/GLOBAL.md` AD-13 |
| 한국어 UI (3.6) | `2026_08_03/GLOBAL.md` 6절 |
| 데이터 불변 (5.2) | `README.md:107`, `2026_08_03/GLOBAL.md` 6절 |
| Alg.svelte 한 곳 (4.2) | `2026_08_03/GLOBAL.md` D-3, `README.md:109` |
| 화면 구성·흐름·진행·판정 (1·2·3.1·3.2) | **이 SPEC (2026_08_24-navigation) 이 정한 값** |

**라우트 축은 여기에 옮기지 않는다** — SVELTE.md 의 "라우트 축" 절이 이미 정본. 상단 도입부에 "라우트 축은 `.dc_workspace/SVELTE.md` 의 라우트 축 절" 이라고 한 줄만.

### 2. `CLAUDE.md` (신규 · 저장소 루트)

SPEC 부록의 초안을 그대로 쓴다 (SPEC "부록 · 루트 CLAUDE.md").

- [ ] 저장소 **루트** 에 만든다. `.claude/CLAUDE.md` 가 아니다 — 그 폴더는 `.gitignore` 에 걸려 있다
- [ ] 길이를 짧게 유지 — 매 세션 문맥에 실린다
- [ ] 기본 규칙은 담고 상세는 포인터 (SPEC 결정 7)
- [ ] 문서 지도가 있어야 하고, 모든 정본 문서로 가는 길이 여기서 출발한다

SPEC 초안 그대로:

```
# cube-study

3BLD 학습 PWA. SvelteKit 2 + Svelte 5(룬) + adapter-static. 서버가 없다 —
전부 프리렌더한 정적 파일이다.

## 문서 지도

- .dc_workspace/CONVENTIONS.md  화면 규약 (구성·흐름·표기). 아래 요약의 정본
- .dc_workspace/SVELTE.md       프레임워크 규약 + 라우트 축. 화면 코드 전에 본다
- README.md                     데이터·도메인 함정, 실행·테스트·배포
- .dc_workspace/CONTINUE.md     지금 어디까지 왔나
- .dc_workspace/BACKLOG.md      다음에 할 일과 그 경로

## 화면을 만들거나 고칠 때

- 세로 순서: 되돌아가기 → <h1> 화면 이름 → 설정 → 본문 → 판정 → 결과 →
  진행 버튼 → 형제 탭. 순서는 고정이다. 단계에 따라 접었다 펴되 자리를 옮기지 않는다
- <h1> 은 화면 이름이다. 문제나 데이터를 <h1> 에 넣지 않는다
- 진행 버튼은 언제나 본문 최하단이다. 단계마다 누를 수 있는 것 하나만 보인다.
  이름은 data-{동작}
- 설정은 **측정하는 판**이 도는 동안만 접고(CSS) 잠근다(disabled). 둘은 따로다.
  기록을 안 남기는 화면은 잠그지 않는다
- 접는 것이 기본이다. 자리를 예약하는 것은 하이드레이션이 흔들리는 자리에만
- 접기는 CSS 로 한다. {#if} 로 DOM 에서 빼지 않는다
- 단계는 data-stage 로 낸다. 검사와 CSS 가 같은 신호를 본다
- 색만으로 알리지 않는다. 같은 자리에 문구가 함께 선다
- SSR 과 CSR 의 요소 구성·개수·크기가 같아야 한다 — 두 번 밟은 함정이다
- UI 문구는 사실만 적는다. 백분율·격려·게이미피케이션 금지

## 새 화면을 어디 둘 것인가

- URL 은 /{퍼즐}/{종목}/{방법}/{세트}/{기능} 축에 앉힌다. 없는 축은 건너뛴다
  (예: /3x3/bld/3style/corner/lookup, /3x3/bld/trace)
- 부분집합은 경로를 가르지 않는다. 방법을 새로 파는 기준은 알고리즘 집합이 다른가
- 하단 탭은 기능 칸만 다른 화면끼리다. 상세는 목록의 탭을 물려받는다
- 상세와 예외는 .dc_workspace/SVELTE.md 의 "라우트 축"

## 코드

- 룬만 쓴다. export let · $: · on:click · <slot> · svelte/store 금지
- 서버 기능은 존재하지 않는다 (+page.server · +server · form actions · 서버 훅)
- 알고리즘 렌더는 Alg.svelte 한 곳. {@html} 금지
- 데이터의 알고리즘을 코드가 만들거나 고치지 않는다
- 배포는 deploy/README.md. 릴리스는 ./deploy/release.sh 하나뿐이다
```

**한 홉이면 닿아야 한다** (FR-NAV-16). 위 초안이 이미 이 규칙을 지킨다 — 세로 순서, `<h1>`, 진행 버튼, 설정 잠금 등이 규칙 한 줄로 담겨 있고, "상세는 SVELTE.md" 같은 링크가 붙는다.

### 3. `README.md:111` (수정)

지금 그 줄은:

> 배경과 설계 근거는 `.dc_workspace/handoff/`와 `.dc_workspace/2026_08_03-corner-3style/`에 있다.

- [ ] 이 줄 위에 새 줄을 추가 (혹은 이 줄을 확장): 화면 규약이 `.dc_workspace/CONVENTIONS.md` 에 있다는 안내
- [ ] 상세는 `CONVENTIONS.md` 로 가고, 진입점은 `CLAUDE.md` 이지만 README 는 사용자·기여자가 보는 문서라 두 곳을 함께 가리키는 것이 자연스럽다

SPEC FR-NAV-15 각주: `README.md:111` 이 이미 `.dc_workspace/` 를 설계 근거의 자리로 가리키고 있으므로 그 줄에 `CONVENTIONS.md` 를 이름으로 더하는 것으로 끝난다.

편집 형태 (권장):

```markdown
배경과 설계 근거는 `.dc_workspace/handoff/`, `.dc_workspace/CONVENTIONS.md` (화면 규약),
그리고 `.dc_workspace/2026_08_03-corner-3style/` 에 있다.
```

`## 주의` 섹션은 **손대지 않는다** (SPEC FR-NAV-15 각주: "거기 있는 것은 데이터·도메인 함정이고 독자가 다르다").

### 4. `CHANGELOG.md` (수정)

지금 `## [0.4.2]` 까지만 있다. `deploy/release.sh:48-51` 이 해당 버전 항목 없이는 릴리스를 거부한다 (SPEC 각주).

- [ ] `## [0.4.2] - 2026-08-18` 위에 `## [0.5.0] - YYYY-MM-DD` 항목 신설
- [ ] 날짜는 릴리스 시점에 사용자가 채운다. **오늘 날짜로 미리 박아두지 않는다** — 릴리스는 사용자가 명시적으로 승인
- [ ] 항목 내용:

```markdown
## [0.5.0] - YYYY-MM-DD

### 추가

- **트레이싱 훈련.** 랜덤 스크램블 → 3D 큐브 표시 → 사용자가 타깃 열 입력 → 실행 모델로 채점. 코너·엣지·양쪽(both) 세 대상, 보고 따라가기·외운 다음 입력 두 모드, 관례 A·B 두 표시. 기록은 `localStorage` 에 최근 50 건. `/3x3/bld/trace`.

- **홈.** `/` 가 홈이 되어 네 기능을 카테고리(3BLD) 아래 이름과 한 줄 설명으로 나열한다. 진도·통계·환영 문구 없음.

### 변경

- **URL 축이 바뀌었다.** 새 축은 `/{퍼즐}/{종목}/{방법}/{세트}/{기능}` — 조회는 `/3x3/bld/3style/corner/lookup`, 기준공식은 `.../algs`, 퀴즈는 `.../quiz`, 트레이싱은 `/3x3/bld/trace`. 옛 경로(`/`, `/anchors`, `/quiz`, `/trace`)로 오는 요청은 nginx `try_files` 가 홈으로 떨어뜨린다. 옛 북마크는 깨지지 않고 홈이 뜬다.

- **화면 규약을 통일했다.** 각 기능 화면에 `<h1>` 이 화면 이름으로 하나. 홈으로 가는 링크가 화면 상단에. 진행 버튼 이름이 `data-{동작}` 규약. 판정 줄이 `data-verdict` · `data-kind` · `data-result` 규약. 자세한 것은 `.dc_workspace/CONVENTIONS.md`.

- **하단 탭이 형제만 담는다.** 조회·기준공식·퀴즈 셋만 서로의 탭에 뜬다. 트레이싱은 형제가 없어 탭바가 안 뜬다. 홈에도 없다.

- **퀴즈의 `<h1>` 이 케이스 코드에서 "퀴즈" 로 바뀌었다.** 케이스 코드는 본문 요소로 내려갔다 — 문제이지 화면 이름이 아니다.

### 문서

- **`.dc_workspace/CONVENTIONS.md` 신설.** 화면 규약의 정본. 흩어져 있던 규약(프리렌더 불변식·색 이중 부호화·터치 44px 등)을 한 곳으로 옮겼다.

- **저장소 루트 `CLAUDE.md` 신설.** 매 세션 실리는 규칙 요약. 정본 문서 다섯으로 가는 지도.

- **`.dc_workspace/SVELTE.md` 는 이미 정본이다.** 이 릴리스에서 손대지 않았다.
```

### 5. `.gitignore` 확인

- [ ] `.claude/` 가 무시되고 있는지 확인 (지금 그렇다). 새로 만든 저장소 루트 `CLAUDE.md` 는 무시되지 않아야 함 — 파일 이름이 대소문자로 갈리므로 `.gitignore` 의 `CLAUDE.md` 항목이 있는지 확인. 없으면 그대로

### 6. `SVELTE.md` — **손대지 않는다**

**페이즈 5 는 `SVELTE.md` 를 다시 쓰지 않는다** (사용자 지시 · SPEC 각주). 2026-08-26 에 이미 정본으로 만들었고, 이 페이즈가 하는 일은 `CLAUDE.md` 에서 그리로 가는 길을 내는 것뿐이다.

## 검증

- [ ] `pnpm check` 통과
- [ ] `pnpm test` 통과
- [ ] `pnpm test:e2e` 통과
- [ ] `pnpm build` 통과
- [ ] `CLAUDE.md`, `CONVENTIONS.md` 안의 링크가 실재 파일을 가리키는지 사람 확인
- [ ] `deploy/release.sh --dry-run` 시늉 (실행 안 함) — CHANGELOG 형식이 릴리스 스크립트의 요구를 만족하는지 확인. 스크립트가 `## [0.5.0]` 헤딩을 찾을 수 있어야 함

## 남기지 않을 것

- **SVELTE.md 를 안 건드림** — 이미 정본
- **README `## 주의` 절을 안 건드림** — 데이터·도메인 함정. 규약이 아님
- **일회용 GLOBAL.md 를 소급 수정하지 않음** — 지나간 작업의 기록이고, 옮겨간 규약은 CONVENTIONS.md 가 정본이 됨 (FR-NAV-15)
- **릴리스 날짜를 미리 박지 않음** — 릴리스는 사용자 명시 승인 뒤
- **`.claude/CLAUDE.md` 를 안 만듦** — `.gitignore` 에 걸려 있어 공유되지 않는다. 저장소 루트가 정답 (FR-NAV-16)
