# 이어서 하기 — 0.5.0 트레이싱

**스냅숏이다. 정본이 아니다.** 각 항목이 가리키는 SPEC·이슈·코드가 정본이고,
이 문서는 "지금 어디까지 왔고 다음에 무엇을 하면 되는가" 만 적는다.
내용이 코드와 어긋나면 코드가 맞다.

마지막 갱신: 2026-08-26 (페이즈 1~5 완료)

---

## 1. 지금 상태

| | |
|---|---|
| 브랜치 | `feat/0.5.0` (origin 에 있음) |
| 워크트리 | `~/Documents/cube-study-feature-tracing` — main 워크트리(`~/Documents/cube-study`)가 아니다 |
| 배포된 커밋 | `21af2d83` — https://cube.siot-ieung.duckdns.org. **로컬 코드가 그보다 훨씬 앞서 있다** |
| 화면 버전 표시 | `0.4.2` (브랜치의 `package.json` 이 아직 0.4.2다. **구분은 커밋 해시로 한다**) |
| 검사 | 단위 471 · e2e 304 전부 통과 |
| 릴리스 가능한가 | **아니다.** `CHANGELOG.md` 에 `## [0.5.0]` 항목이 없어 `deploy/release.sh:48-51` 이 거부한다 |

배포는 릴리스가 아니라 임의 ref 올리기다.

```bash
cd ~/Documents/cube-study-feature-tracing
./deploy/deploy.sh feat/0.5.0     # 지금 방식 — 버전·태그를 건드리지 않는다
```

롤백은 `./deploy/deploy.sh v0.4.2` 한 줄이다. 0.5.0 은 새 `localStorage` 키
(`trace.*`)만 쓰고 암기 진도 키를 건드리지 않으므로 롤백이 데이터 안전하다.

---

## 2. 이번 작업에서 실사용 피드백으로 바뀐 것

전부 배포되어 있다. 커밋 메시지에 근거가 있다.

| 커밋 | 무엇 |
|---|---|
| `7c717c7` | 세션 설정을 시작하면 접는다 · 훈련 중 하이라이트를 버퍼만 남기고 정답 경로는 결과 단계로 · 비틀림 관례 토글을 결과 패널로 · 기록 표를 초/타깃으로 |
| `c4bb0db` → `e6fb839` | 엣지 버퍼. **토글을 만들었다가 되물렀다** — DF 고정이 요구였다 |
| `e6fb839` | 결과 단계에서 입력 자판을 접는다 (입력칸은 남긴다) |
| `21af2d8` | 시작 전에 입력칸·자판·판정 줄을 접는다 · 큐브 높이 상한의 고정분을 26rem→28rem |

### 되돌린 판단 하나

**엣지 버퍼에 토글을 붙인 것이 과했다.** 요구는 "DF 로 고정" 이었고,
고를 수 있는 반대편(UF)이 이 앱의 과정에 없으므로 아무도 안 누르는 선택지가
세로만 먹는다. `e6fb839` 이 되돌렸고, `tests/e2e/trace-input.spec.ts` 의
T4-10 에 **"버퍼를 고르는 토글이 화면에 없다"** 는 검사를 넣어 다시 생기는 것을 막았다.

### 2026-08-26 피드백 (배포 전)

| 무엇 | 어디 |
|---|---|
| **버퍼 하이라이트를 걷었다.** 실물 큐브에는 버퍼 자리에 표시가 없다. 초록·흰색으로 방향을 잡고 버퍼를 찾는 것까지가 훈련이다 | `domain/tracing.ts` 의 `buildMarks`, `trace/+page.svelte` 의 `marks` |
| 그래서 **훈련 중 큐브에는 강조가 하나도 없다.** `buildMarks` 는 결과 단계에서만 돈다 (정적 검사가 못 박는다) | `tests/unit/tracing.test.ts` T5-1 |
| `MARK_PALETTE` 이 두 갈래(현재·지나감)로 줄었다. `buildMarks` 가 `meta` 를 안 받는다 — 받을 이유가 버퍼뿐이었다 | 같은 파일 |
| E2E 의 "그려졌다" 신호가 버퍼 청록 → **스티커 색** 으로 바뀌었다. 흰색은 뺀다 (큐비 테두리 선이 흰색이라 회색 단계에도 잡힌다) | `tests/e2e/trace-noleak.spec.ts` 의 `painted()` |
| **암기 숨김·역공식 숨김 아이콘이 눈에서 뇌·되돌이 화살표로 바뀌었다.** 나중에 "아이콘만 보기" 를 만들 때 둘 다 눈이면 구분이 사라진다. 가리는 중인가는 공통 사선이 계속 말한다 | `routes/anchors/[code]/+page.svelte` |

### 실측으로 잡은 것

- 큐브 높이 상한의 고정분이 **상단 바 49px 을 빠뜨리고 있었다** (26rem → 28rem)
- **정사각 영역은 폭에도 묶인다** — 390px 폭에서 큐브가 361px 이라 높이 계산이 안 닿는다
- 세로 맞춤 현황 (idle 문서 높이 / 진행 중 패드 아래끝)

  | | idle | 진행 중 |
  |---|---|---|
  | 390×844 | 847/844 | 821/844 OK |
  | 412×915 | 915/915 OK | 843/915 OK |
  | 412×839 | 869/839 | 843/839 |
  | 375×667 | 704/667 | 678/667 |

  390 이상에서 "거의 스크롤 없음" 이 된다. 그 아래는 큐브가 하한(180px)에 가까워
  더 줄일 곳이 없다 → **#28**

---

## 3. navigation 작업은 끝났다 (페이즈 1~5 완료)

`.dc_workspace/2026_08_24-navigation/` 의 SPEC·GLOBAL·PHASE 문서가 정본이고,
다섯 페이즈가 다 구현됐다. 커밋 `d30dce7` · `66c4f36` · `6305f31` · `98a4861`.

| 페이즈 | 무엇이 바뀌었나 |
|---|---|
| 1 | 화면 넷을 `/3x3/bld/...` 축으로, `/` 는 홈, 하단 탭을 경로에서 계산 |
| 2 | `<h1>` 을 화면 이름으로 통일 (퀴즈의 케이스 코드는 본문으로) |
| 3 | 진행 버튼을 본문 최하단으로, 판정 줄·버튼 이름·`data-stage` 규약 통일 |
| 4 | `tests/e2e/conventions.spec.ts` · `tests/unit/routes.test.ts` 신설 |
| 5 | `CONVENTIONS.md` · 루트 `CLAUDE.md` · `CHANGELOG` 의 `[0.5.0]` |

검사: 단위 487 · e2e 327 · `pnpm check` 0 · `pnpm build` 통과.

### 옮기다 잡은 사고 둘 (둘 다 배포에 영향)

**깊은 경로에서 자산이 404 였다.** `paths.relative` 기본값이 페이지 기준
상대경로를 내는데, 서비스워커가 `navigateFallback: '/'` 로 index.html 을 내주면
브라우저가 그것을 엉뚱한 깊이로 푼다. 라우트가 한 칸이던 0.4.2 까지는 우연히
맞아떨어졌다. `paths.relative: false` 로 고쳤다.

**`?c=` 가 붙은 주소가 홈으로 떨어졌다.** 프리캐시 키에 쿼리가 없어 이동 요청과
안 맞고, 그때 폴백이 `/` 다. 예전에는 그 폴백이 조회 화면 자신이라 사고가
아니었다. `ignoreURLParametersMatching: [/.*/]` 로 고쳤다.

둘 다 서비스워커가 제어를 잡은 **두 번째 이동**에서만 드러난다. 배포 후 확인할 때
한 번 열어보고 끝내지 말 것 — 같은 주소를 두 번 열어야 한다.

### 남은 일

- **CHANGELOG 의 `[0.5.0]` 날짜가 "미정" 이다.** 릴리스할 때 박는다
- 배포는 아직 안 했다. 프리캐시 목록이 통째로 바뀌므로(라우트 다섯이 옮겨갔다) `deploy.sh` 의 전수 대조가 이번엔 실질적인 검사다
- 진행 버튼이 자판 아래로 내려가면서 세로가 44px 더 든다. 짧은 뷰포트에서 한 화면 fit 이 어떻게 되는지 실기로 재야 한다 (#28)

## 4. 열린 이슈

| | |
|---|---|
| **#28** | 짧은 뷰포트에서 한 화면 fit 이 무너진다. 위 실측표와 세 가지 대안(JS 실측 / 플렉스 레이아웃 / 포기 문턱)이 이슈에 있다 |
| **#29** | 갤럭시 Z 폴드 폼팩터. Z Fold 8 실측 사양(커버 5.5" 1248×1972 10:16, 내부 7.6" 2448×1848 4:3)이 이슈에 있다. **커버는 비율은 세로로 길지만 접은 본체 높이가 123.9mm 로 통상보다 낮다** |
| #27 | 정보 구조. 이번 SPEC 이 이것을 착수한 것이다 |
| #16 · #17 | 엣지 3-style 데이터 · M2 참조표. 둘 다 엣지 버퍼 DF 고정의 근거다 |
| #26 | 색각 고대비 모드. 디자인 시스템 착수의 가장 강한 신호다 (`BACKLOG.md`) |

---

## 5. 결정된 것과 그 근거가 어디 있나

| 결정 | 정본 |
|---|---|
| 페이지/카테고리 구성, 화면 규약 14+3개 | `2026_08_24-navigation/SPEC.md` |
| 규약 문서는 `.dc_workspace/CONVENTIONS.md`, `CLAUDE.md` 는 규칙을 담고 근거만 링크 | 같은 SPEC FR-NAV-15·16 |
| 기계로 확인 가능한 규약은 검사로 못 박는다 | 같은 SPEC FR-NAV-17 |
| 디자인 시스템은 아직 아님 + 착수 신호 셋 | `BACKLOG.md` 의 "디자인 시스템" 절 |
| 트레이싱 훈련 자체의 요구 | `2026_08_20-tracing/SPEC.md`, `GLOBAL.md` |
| 기록에 `targetCount`·`twistConvention` 을 남기는 이유 | `domain/tracing.ts` 의 `msPerTarget` 주석 — 보여줄 값이 아니라 나눗셈의 분모다 |
| 훈련 중 하이라이트를 버퍼만 남긴 이유 | `routes/trace/+page.svelte` 의 `marks` 주석 |
| 엣지 버퍼가 DF 인 이유 | 같은 파일 `EDGE_BUFFER` 주석 |

---

## 6. 밟지 말 것

- **`{#if}` 로 화면 요소를 넣고 빼지 않는다.** SSR/CSR 요소 개수가 갈리면 하이드레이션이
  흔들린다. 이 저장소가 두 번 밟았다. 접는 것은 CSS 로 한다 (SPEC FR-NAV-18)
- **버퍼 리터럴을 엔진·도메인에 적지 않는다.** 정적 검사가 `src/lib/cube/trace.ts` 와
  `src/lib/domain/tracing.ts` 를 본다. 조립부(`routes/trace/+page.svelte`)만 알아도 된다
- **e2e 테스트 제목에 템플릿 리터럴을 쓰지 않는다.** `tests/unit/e2e-tags.test.ts` 의
  파서가 문자열 리터럴만 읽고, 제목을 못 읽으면 `@viewport` 태그도 못 읽어 그 테스트가
  mobile 프로젝트에서 조용히 빠진다
- **e2e 픽스처가 앱 기본값에 기대게 두지 않는다.** 엣지 버퍼 기본값을 바꿨을 때
  기대값이 통째로 어긋난 적이 있다. 전제는 픽스처가 적는다
- **합성 클릭(`dispatchEvent`)으로 `disabled` 를 확인하지 않는다.** 브라우저가 애초에
  배달하지 않는 이벤트라 사용자가 겪는 일과 무관한 답이 나온다
