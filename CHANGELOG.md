# 변경 이력

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르고,
버전은 [유의적 버전](https://semver.org/lang/ko/)을 따른다.

버전의 정본은 `package.json` 하나이며 빌드 타임에 앱으로 주입된다.

## [0.3.0] - 2026-08-09

케이스별 암기 표시.

### 추가

- **암기 체크박스.** 조회 카드와 기준 상세에서 케이스를 외웠는지 표시한다. 암기 상태는 케이스 × 표기(setup / direct) 단위로 저장한다 — 같은 케이스라도 셋업형과 최적형은 다른 무브열이라 따로 세어야 하고, 퀴즈에 두 입력 방식이 있으므로 한 덩어리로 셌으면 setup 만 외운 케이스가 direct 퀴즈에 나온다. 조회 카드의 체크박스는 지금 보고 있는 표기의 상태를 뜻하고, 기준 상세는 그 화면이 셋업 무브를 나열하므로 항상 setup 기준이다.

- **기준별 진도.** 기준 목록과 기준 상세에 setup 기준 암기 수를 숫자로 표시한다. 진도바·퍼센트·축하 문구는 넣지 않는다 — 사용할 때마다 읽는 숫자라 밀도가 높아야 한다.

- **"암기 숨김".** 기준 상세에서 암기 완료한 케이스를 가릴 수 있다. 진도와 같은 줄 오른쪽에 스위치로 두고, 상태는 눈 아이콘이 알린다 — 켜면 눈에 사선이 생긴다. 요소를 DOM 에서 지우지 않고 CSS 로 감춘다 — 요소가 하이드레이션 순간 사라지면 목록이 축소되며 화면이 밀리는데 이 프로젝트가 이미 두 번 밟은 함정이다. 숨겨도 진도 분모는 전체를 유지한다 — 분모가 줄면 진도가 아니라 남은 개수가 된다.

- **퀴즈 "암기한 것만".** 현재 입력 방식의 암기 목록으로 출제 풀을 좁힌다. 암기가 0개면 토글은 켜 둔 채로 안내를 띄운다 — 조용히 전체에서 출제하면 필터가 동작한 줄 안다.

- **정보 모달 "암기 표시 전체 해제".** 두 번 눌러야 실행된다. 브라우저 `confirm()` 은 쓰지 않는다 — 모달 위에 또 모달이 뜨고 문구를 앱이 통제하지 못한다. 모달을 닫으면 확인 상태를 되돌린다 — 남겨두면 다음에 열었을 때 첫 클릭이 곧바로 실행으로 보인다.

- 암기 데이터는 `localStorage` 에 저장된다. 스키마 버전을 포함하며 기기 간 동기화는 없다.

### 변경

- **기준 상세 목록에서 무브 수를 뺐다.** 옆에 보이는 것은 셋업뿐인데 수는 셋업+기준+역셋업을 상쇄까지 적용한 최종 길이라 단위가 다르다. 셋업 3수 옆에 8수가 적혀 있으면 "3수인데 왜 8수" 가 된다 — `CP` 가 실제로 그렇다 (`3+8+3=14 → 상쇄 6 → 8`).

- **기준 상세의 머리를 상단에 고정한다.** 기준공식·진도·토글·열 머리글까지. 이 화면은 기준을 보며 셋업을 익히는 화면이라, 목록을 훑는 동안 기준이 화면 밖으로 나가면 각 줄의 셋업이 무엇에 붙는 셋업인지 알 수 없어 매번 위로 올라가 확인하게 된다. 열 머리글을 빼면 스크롤 뒤 오른쪽 체크박스 열이 이름 없는 칸이 된다. 다만 낮은 화면(가로 방향 폰 등)에서는 고정하지 않는다 — 머리가 280px 남짓이라 세로 420px 화면에서는 훑을 자리가 사라진다.

- **목록 체크박스를 회색 칸 안으로.** 칸 바깥 왼쪽에 떠 있어서 어느 줄 것인지 눈으로 묶이지 않았다. 배경과 테두리를 `<a>` 에서 `<li>` 로 올려 해결한다 — 체크박스는 여전히 `<a>` 밖 형제다. 안에 넣으면 체크 클릭이 링크 이동을 일으키고, `preventDefault` 로 막으면 터치와 접근성이 지저분해진다. 보이기에만 한 칸이고 이벤트는 그대로 분리되어 있다.

- **줄마다 라벨을 달지 않고 열 머리글을 한 번만 적는다.** 112줄에 "암기" 를 112번 적으면 그게 배경 소음이 된다. 머리글은 `aria-hidden` 이고, 실제 접근성 이름은 체크박스마다 `{케이스} 암기` 로 붙였다 — 스크린리더는 열 머리글을 읽어주지 않으므로 머리글에 기대면 이름이 없는 것과 같다.

- **아이콘 버튼에 이름을 붙였다.** 상태는 색과 아이콘이, 정체는 글자가 맡는다 (상단 바 "화면 켜기", 목록 "암기 숨김"). 정보(i)만 글자 없이 둔다 — 관례가 굳어 있어 읽힌다.

- 상단 바 라벨의 접힘 기준을 400px 에서 **330px** 로 내렸다. 실측하니 412px 에서 버튼 합이 216px 이고 설치 버튼까지 떠도 320px 에 들어간다. 400px 기준은 과해서 흔한 폰 폭(360~412)에서 글자가 전부 사라지고 있었다 — 정작 라벨이 필요한 화면에서.

### 데이터

- **`corner-UBL.json` 을 schemaVersion 7 로 교체했다.** 핑거링 지적에 따라 공급자가 목적함수를 바꿔 전면 재설계했다. 총 무브 수 하나로 고르던 것을 **셋업 길이 → 무브 수 → 손가락 점수** 우선순위로 바꾸고, 기준공식 후보를 **F/B 미사용으로 제한**했다.

- **기준공식이 5개에서 10개로 늘었다** (`GC BU DO CH IT OI SC VJ SV TH`, 전부 `R·U·D·L` 만 쓴다). 늘리는 것이 비용인데도 나머지 지표가 전부 좋아졌다 — 기준공식의 F/B 1/5 → **0/10**, 셋업 3수 151건(40%) → **6건(1.6%)**, 셋업의 F/B 162건(43%) → **104건(28%)**, setup 평균 10.43 → **10.05수**. v6 의 기준 중 살아남은 것은 `BU` 하나다.

- 이 과정에서 v5 가 지나온 6개→5개 축소는 되돌려졌다. 기준 수는 외울 양이라 적을수록 좋다는 전제로 줄였던 것인데, 실제로 불편했던 것은 개수가 아니라 **손에 안 붙는 무브**였다.
- 배정 규칙도 함께 바뀌었다 (`meta.assignmentRule`). v5 의 "가장 짧은 알고리즘" 단일 기준에서 셋업 길이·무브 수·손가락 점수의 우선순위로. 378 전 케이스에 예외 없이 적용되고, 기준 없이 전용 알고리즘을 쓰는 케이스는 여전히 0건이다.
- 담당이 고르게 퍼졌다 (44/44/42/40/38/38/38/38/28/28). 기준 목록의 순서는 `meta.anchorLearnOrder` 를 그대로 따르며, v7 의 그 순서는 담당 수 순이 아니다 — 순서를 무엇으로 매기는지는 데이터가 정할 일이다.
- 새 필드: `setup.setupUsesFB`, `anchors[].usesFB`, `meta.anchorConstraint`·`setupLengthDist`·`setupFBCount` (v7). 그 앞 버전에서 들어온 `setup.isAnchorCase`, `anchors[].ownCases`·`soloReach`·`soloAvgMoves`, `meta.learningCurve`·`coverageNote`·`assignmentRule`·`partitionNote` 도 그대로다. 타입에 반영했고 앱은 아직 화면에 쓰지 않는다. `strict.cancelNote`(설명 문자열)는 빠졌는데 코드가 읽지 않던 필드다.
- **화면 코드는 한 줄도 고치지 않았다.** 기준의 개수·이름·순서를 어디에도 적어두지 않았기 때문이다 — 5개가 10개가 되고 이름이 8개 갈렸는데 화면이 그대로 따라갔다.

- 대신 **케이스를 지목해 둔 테스트 두 건이 깨졌다.** `grade.test.ts` 는 "다른 무브 열도 정답" 을 보이려고 `FS` 를 박아뒀는데 v7 에서 `FS` 의 셋업 경로가 direct 와 같은 무브 열로 떨어졌다. `anchor-sticky.spec.ts` 는 `/anchors/IV` 를 열었는데 `IV` 가 사라졌다. 둘 다 데이터에서 조건에 맞는 것을 골라 쓰도록 고쳤다 — 확인하려는 것은 성질이지 특정 케이스·기준이 아니다.
- 교체본을 우리 시뮬레이터로 378 케이스 전수 재검증했다. 공급자 검증(1512/1512)을 그대로 믿지 않는다. F/B 개수·셋업 길이 분포·평균 무브도 동봉 문서를 믿지 않고 데이터에서 직접 세어 대조했다 — 전부 일치한다.
- **`strict.cancels` 계산 버그가 함께 고쳐졌다.** v3~v5 는 이 값을 `(strict.moves - moves) / 2` 로 냈는데, 상쇄가 늘 무브 둘을 지운다고 가정한 것이라 틀렸다 (`R R → R2` 는 하나만 준다). 378×2 중 470건이 어긋나 있었다. 알고리즘과 배정은 한 건도 바뀌지 않았다.
- 새 필드 `setup.setupMoves`·`setup.breakdown`. 셋업 길이와 최종 무브 수가 안 맞아 보이는 것을 설명하는 용도다 (`"3+8+3=14 → 상쇄 6 → 8"`).
- 기록은 `data/schema-history/` 에 남긴다 (`corner-UBL.v7.json`, `MIGRATION-v3-to-v7.md`). 동봉된 `corner-UBL-alternatives.v7.json`(케이스별 10개 기준 각각의 경로)은 보관만 하고 앱은 쓰지 않는다 — 쓰려면 기준 단위의 학습 상태가 필요한데 이번 암기 표시는 케이스 단위다.

### 설계 메모

역케이스 자동 연동은 하지 않는다. `AB` 를 외웠다고 `BA` 를 외운 것으로 처리하면, 두 케이스는 실제로 외워야 할 내용이 다르므로 진도가 실제와 어긋난다.

프리렌더 대응은 이 기능의 핵심 제약이다. 체크박스·진도 숫자는 SSR 산출물에 이미 렌더되고 하이드레이션 때 값만 채워진다. 요소가 뒤늦게 생기거나 사라지면 화면이 밀린다 — 이미 두 번 밟은 함정이라 — 숨기기는 `{#if}` 대신 CSS 를 쓰고, 진도 숫자는 자릿수가 달라도 폭이 밀리지 않게 자리를 예약한다.

## [0.2.1] - 2026-08-08

앱 동작은 그대로다. 검사·감시·배포 도구만 손봤다.

### 추가

- **CI.** PR·main push 마다 타입 검사·단위·E2E·배포 스크립트 검사. 없던 것을 처음 넣는다 — E2E 가 pnpm 전환 이후 여러 커밋 동안 죽어 있어도 아무도 몰랐던 이유가 이것이다
- **사이트 상시 점검.** 하루 한 번 인증서 만료 임박·기본 경로 응답·프리캐시 전수 200 을 확인하고, 어긋나면 알림이 온다. 인증서가 6개월간 만료 상태였고 `acme.sh --list` 는 정상으로 보였던 전례에 대한 대비다
- 배포 스크립트 셸 테스트. 가짜 저장소로 돌아서 홈서버·ssh·네트워크가 필요 없다

### 변경

- 서버에서 도는 부분을 `deploy/remote.sh` 로 분리했다. heredoc 안에 있으면 테스트할 수 없다
- `deploy/lib.sh` 에 `resolve_commit` 를 두고 `--verify` 를 붙였다. 없으면 해석 실패 시 입력 문자열을 그대로 뱉고 성공한 척해서, 엉뚱한 "어긋남" 진단이 나온다

### 고침

- **서버가 옮겨간 태그를 따라가지 않았다.** `git fetch --tags` 는 이미 있는 태그를 갱신하지 않으면서 종료 코드 0 을 준다. 롤백으로 태그를 지웠다 다시 만들면 서버는 옛 커밋을 배포하고도 성공한 척한다. v0.2.0 릴리스가 실제로 이 상태로 반쯤 나갔다. `--force` 를 붙였고, 이 동작을 셸 테스트로 못박았다

## [0.2.0] - 2026-08-08

쓰다 걸린 것 세 가지.

### 추가

- **상위 이동 링크.** 기준 상세에서 기준 목록으로, 기준을 거쳐 들어간 조회 화면에서 그 기준으로 돌아간다. 목적지를 버튼에 적어두므로 누르기 전에 어디로 가는지 안다. 역케이스 링크를 타도 출처가 남는다 — 역케이스는 378/378 이 같은 기준에 속하므로 목적지가 여전히 그 케이스를 담고 있다
- 기준공식 목록과 상세에 **`[A, B]` 커뮤테이터 표기**. 무브열과 함께 보여준다 — 이 화면만 켜두고 기준을 외우는 용도라 구조가 먼저 보여야 한다
- `deploy/release.sh` — 버전 갱신·태그·푸시·배포·검증을 한 번에. 태그와 `package.json` 버전이 어긋나는 것을 막는다

### 변경

- 기준별 파생공식 목록을 **셋업 수 순에서 알파벳순으로**. 셋업 수 순은 찾는 케이스가 어디 있는지 알 수 없어 매번 전체를 훑게 된다

### 고침

- **E2E 가 pnpm 전환 이후 한 번도 돌지 않았다.** `pnpm run preview -- --port 4174` 에서 pnpm 이 `--` 를 스크립트에 그대로 넘겨 포트 지정이 무시됐고, playwright 는 다른 포트를 기다리다 60초 뒤 죽었다. 증상이 "webServer 타임아웃"이라 빌드가 느린 것으로 보인다 — 실제 빌드는 3.4초다
- **annotated 태그를 배포하면 커밋 대조가 어긋났다.** `git rev-parse <태그>` 가 커밋이 아니라 태그 객체 SHA 를 준다. 서버는 올바른 커밋을 배포하는데 로컬만 다른 값으로 비교해서, 검증이 목적인 스크립트가 HTTP 검증과 프리캐시 대조를 통째로 건너뛰고 실패로 끝났다

### 설계 메모

히스토리 기반 뒤로가기는 쓰지 않는다. 설치된 앱은 화면 이동이 히스토리를 쌓지 않고 교체하므로(`replacestate`) 되짚을 스택이 없고, 되살리면 뒤로가기가 아까 본 케이스를 하나씩 지나는 문제가 돌아온다. 스택이 있더라도 "뒤로" 는 눌러봐야 어디인지 안다. 출처는 히스토리가 아니라 링크의 쿼리(`?from=`)로 넘어오므로 새로고침해도, 앱을 껐다 켜도 유지된다.

## [0.1.0] - 2026-08-08

코너 3-style (UBL 버퍼) 첫 릴리스. 378 케이스 전부를 담는다.

### 추가

**조회**

- 스티커 2글자로 케이스 조회. 소문자·딥링크(`/?c=LB`) 지원
- Speffz(A~X) 밖 문자는 값에 반영하지 않고 배경 플래시로 알린다. 한글 IME·숫자·기호가 이 규칙 하나로 처리된다
- 무효하거나 미완성인 입력에도 직전 결과를 남긴다. 입력을 다 지우면 치운다
- 화면에 들어오면 입력에 커서가 놓인다
- 두 글자가 차면 키보드를 내린다. 입력을 다시 탭하면 값을 비우고 처음부터 받는다
- direct/setup, structural/compact 표기 토글. 선택은 유지된다
- 인서트·교환·셋업·기준공식을 색으로 구분. 무브 사이에 실제 공백을 넣어 복사해도 구분이 남는다

**기준공식 브라우저**

- 기준 목록을 담당 케이스 수 순으로. 기준별 케이스 목록에서 조회로 이동
- 기준을 거꾸로 돌리는 케이스에 `역` 표시

**퀴즈**

- 입력 방식 두 가지. `direct` 는 18개 무브 버튼으로 알고리즘 전체를, `setup` 은 셋업 무브를 넣고 기준공식을 고른다. 기준을 고르는 순간이 제출이다
- 채점은 문자열 비교가 아니라 큐브 시뮬레이터로 실제 효과를 판정한다. 데이터에 없는 유효한 변형도 정답이 된다
- 정답/오답 외에 "코너는 맞지만 엣지를 건드립니다", "조각 위치는 맞지만 방향이 다릅니다", "3-cycle이 아닙니다" 를 구분해 알려준다
- 정답은 판정 전까지 DOM 에 렌더하지 않는다
- 연속 정답 카운터·배지·격려 문구를 두지 않는다

**공통**

- PWA. 데이터까지 프리캐시해 오프라인에서 조회·브라우저·퀴즈가 모두 동작한다
- 설치 버튼. 브라우저는 설치 팝업을 스스로 띄우지 않으므로 `beforeinstallprompt` 를 잡아뒀다가 누를 때 띄운다. 조건 미달이거나 이미 설치됐으면 버튼도 나오지 않는다
- 새 버전을 자동으로 받아 적용하고 토스트로 알린다. 정보 모달에서 직접 확인할 수도 있다
- 설치된 앱에서 뒤로가기는 "닫기" 하나만 뜻한다. 화면 이동은 하단 탭으로 한다 — 뒤로가기가 방문 기록을 되짚으면 아까 본 케이스를 하나씩 지나며 한참 뒤에야 닫힌다. 실수를 막으려고 두 번 누르게 하고 그 사이 예고 토스트를 띄운다. 브라우저 탭에서는 켜지 않는다
- 화면 자동 꺼짐 방지 토글. 공식을 보며 큐브를 돌리는 동안 화면에 손이 안 닿아 꺼진다
- 시스템/라이트/다크 테마. 렌더 전에 적용해 깜빡임이 없다
- 앱 정보 모달 (버전·커밋 해시)
- 모바일 우선 반응형. 터치 대상은 44px 이상

### 데이터

- `corner-UBL.json` schemaVersion 3. 378 케이스, 기준공식 6개 (`GC TC BU IV KS KG`), 기준 없이 전용 알고리즘을 쓰는 케이스 0건
- 기준공식의 개수·이름·순서·방향을 코드와 테스트 어디에도 하드코딩하지 않는다. 목록은 `meta.anchorLearnOrder`, 없으면 `anchors` 키 순서에서 읽는다
- 378 중 188 케이스가 기준공식을 거꾸로 돌린다 (`setup.usesInverse`). 조회는 뒤집은 무브열과 `GC'` 표기를, 퀴즈는 기준마다 정·역 두 버튼을 제공한다
- v1~v3 스키마와 교체 안내를 `data/schema-history/` 에 기록으로 보관한다. 앱이 읽는 파일은 `src/lib/data/corner-UBL.json` 하나다

### 배포

- 패키지 매니저는 pnpm 10, Node 24. `packageManager` 필드가 있어 corepack 이 버전을 맞춘다
- `deploy/deploy.sh` 하나로 홈서버 배포. 서버가 직접 `git fetch` → 빌드 → docroot 반영을 하므로 커밋되지 않은 로컬 수정이 새어 나갈 수 없다
- 배포 후 스스로 확인한다. 서버가 체크아웃한 커밋이 맞는가, 그리고 **서비스워커 프리캐시 목록의 파일이 전부 실제로 서빙되는가** — 하나라도 404 면 워크박스가 설치를 통째로 롤백해서 오프라인이 죽는데 화면은 멀쩡해 보인다
- 접속은 Tailscale 을 탄다. 집 안팎에서 같은 명령이고 공유기에 열어둔 포트가 없다

### 검증

- 378 케이스 전수 회귀. 각 알고리즘이 해당 케이스를 실제로 푸는가, 엣지를 안 건드리는가, 파생 필드가 실제와 일치하는가
- `S + 기준(방향) + S'` 조립이 데이터의 `setup.strict.alg` 와 378/378 일치
- 스키마 호환 — v2 와 배포본을 같은 도메인 함수에 먹여 확인한다. 앱이 읽을 수 있는 하한은 schemaVersion 2 다 (v1 은 `strict` 필드가 없다)
- 단위 120건, E2E 198건. E2E 는 모바일·데스크탑 두 프로필로 돈다

[0.3.0]: https://github.com/U-lis/cube-study/releases/tag/v0.3.0
[0.2.1]: https://github.com/U-lis/cube-study/releases/tag/v0.2.1
[0.2.0]: https://github.com/U-lis/cube-study/releases/tag/v0.2.0
[0.1.0]: https://github.com/U-lis/cube-study/releases/tag/v0.1.0
